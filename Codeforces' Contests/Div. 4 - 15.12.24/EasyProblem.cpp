/* accepted :) */

#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main (){
    ll testes, n;

    scanf("%lld", &testes);

    while(testes--){
        scanf("%lld", &n);
        printf("%lld\n", n-1);
    }

    return 0;
}